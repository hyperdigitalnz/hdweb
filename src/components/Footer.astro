---
import { SITE, STATS_NOTE, NAV_SERVICES, NAV_COMPANY, REVIEWS } from "../consts";
---
<footer class="bg-neutral text-neutral-content/70">
  <div class="mx-auto max-w-[1160px] px-5 py-12">
    <div class="flex flex-col gap-8 md:flex-row md:items-start md:justify-between">
      <div class="max-w-sm">
        <a href="/" aria-label="HYPERDIGITAL home" class="logo inline-block text-2xl text-neutral-content transition-colors hover:text-primary">HYPER<span class="text-primary">DIGITAL</span></a>
        <p class="mt-3 text-sm">{SITE.tagline}</p>
        <p class="mt-2 text-sm">{SITE.location}</p>
      </div>

      <!-- Both columns render from the shared NAV_* lists in consts.ts, the same source
           as the header and mobile panel, so the nav surfaces can't drift apart. -->
      <nav class="flex flex-col gap-2 text-sm">
        <span class="font-display text-sm uppercase tracking-wide text-neutral-content">Services</span>
        {NAV_SERVICES.map((l) => (
          <a href={l.href} class="transition-colors hover:text-primary">{l.label}</a>
        ))}
      </nav>

      <nav class="flex flex-col gap-2 text-sm">
        <span class="font-display text-sm uppercase tracking-wide text-neutral-content">Company</span>
        {NAV_COMPANY.map((l) => (
          <a href={l.href} class="transition-colors hover:text-primary">{l.label}</a>
        ))}
      </nav>

      <div class="text-sm">
        <a href={SITE.phoneHref} class="font-display text-2xl text-neutral-content transition-colors hover:text-primary">{SITE.phone}</a>
        <p class="mt-2">{SITE.hours}</p>
        <a href={REVIEWS.url} target="_blank" rel="noopener" class="mt-3 flex items-center gap-1.5 transition-colors hover:text-primary">
          <span class="text-primary" aria-hidden="true">★★★★★</span>
          <span><span class="font-bold text-neutral-content">{REVIEWS.rating}</span> · {REVIEWS.count} Google reviews</span>
        </a>
      </div>
    </div>

    <div class="mt-10 flex flex-col gap-4 border-t border-white/10 pt-6 text-xs sm:flex-row sm:items-center sm:justify-between">
      <span class="text-center sm:text-left">© {new Date().getFullYear()} {SITE.name}</span>
      <div class="flex items-center justify-center gap-4 sm:gap-6">
        <img src="/img/google-ads-partner-badge.png" alt="Google Ads Partner" width="240" height="80" loading="lazy" class="max-h-16 w-auto max-w-[44%] object-contain sm:max-h-24" />
        <img src="/img/meta-business-partner-badge.jpg" alt="Meta Business Partner" width="240" height="80" loading="lazy" class="max-h-16 w-auto max-w-[44%] rounded object-contain sm:max-h-24" />
      </div>
      <div class="flex justify-center gap-4 sm:justify-start">
        <a href="/privacy" class="transition-colors hover:text-primary">Privacy</a>
        <a href="/terms" class="transition-colors hover:text-primary">Terms</a>
      </div>
    </div>

    <p class="mt-6 text-xs text-neutral-content/60">* {STATS_NOTE}</p>
  </div>
</footer>
