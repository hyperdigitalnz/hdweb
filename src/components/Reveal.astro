---
// Wraps content in a scroll-reveal container. Animated by the Motion script in Layout.astro.
interface Props {
  delay?: number;
  class?: string;
}
const { delay = 0, class: className = "" } = Astro.props;
---
<div data-reveal data-reveal-delay={delay} class={className}>
  <slot />
</div>
